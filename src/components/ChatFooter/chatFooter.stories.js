import React from "react";
import ChatFooter from ".";

export default {
	title: "UI组件/ChatFooter",
	component: ChatFooter,
};

export const Default = () => (
	<div style={{ marginTop:"40px" }}>
		<ChatFooter/>
	</div>
);
