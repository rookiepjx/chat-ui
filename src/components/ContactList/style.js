import styled from "styled-components";
import StyledContactCard from "components/ContactCard/style"

const StyledContactList = styled.div`
  ${StyledContactCard}{
    margin:10px 0;
  }
`;

export default StyledContactList;

