import styled from "styled-components";
import StyledFileCard from "components/FileCard/style";

const StyledFileList = styled.div`
	${StyledFileCard} {
		margin: 10px 0;
	}
`;

export default StyledFileList;
